/** @param {NS} ns **/
export async function main(ns) {
let target = ns.args[0]
let root = "home"
let rootScan = ns.scan(root)
let visited = []
let next = []

async function scanner(target,root,visited,next){
for ()
}
	
}